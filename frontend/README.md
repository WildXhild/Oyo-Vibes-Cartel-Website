# Frontend (OVC)

Next.js starter for the Oyo Vibes Cartel frontend.

Setup

```bash
cd frontend
npm install
npm run dev
```

Notes
- Connect API calls to the backend at `/api` or set up a proxy.
- Implement pages: events, event/[id], register, dashboard, admin
